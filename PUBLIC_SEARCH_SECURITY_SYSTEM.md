# نظام البحث العام الآمن - التوثيق الشامل

## 📋 نظرة عامة

تم تطوير نظام بحث عام آمن ومتطور يسمح للمستفيدين بالبحث عن بياناتهم باستخدام رقم الهوية الوطنية، مع حماية متعددة الطبقات للبيانات الحساسة.

## ✨ الميزات الرئيسية

### 1. البحث العام المحدود
- السماح لأي شخص بالبحث عن بياناته باستخدام رقم الهوية الوطنية (9 أرقام)
- عرض معلومات محدودة فقط:
  - الاسم الكامل
  - رقم الهوية الوطنية
  - الطرد قيد التوصيل فقط (in_delivery)
- إخفاء جميع البيانات الحساسة في العرض الأولي

### 2. نظام حماية بكلمة مرور (PIN)
- كلمة مرور من 6 أرقام
- إنشاء كلمة المرور عند الطلب الأول لعرض التفاصيل
- تخزين آمن مع تشفير
- نظام محاولات محدود:
  - 5 محاولات فاشلة كحد أقصى
  - قفل الحساب لمدة 30 دقيقة بعد تجاوز الحد
- التحقق من قوة كلمة المرور

### 3. عرض التفاصيل الكاملة
بعد التحقق من كلمة المرور، يمكن عرض:
- جميع البيانات الشخصية
- جميع الطرود (السابقة، الحالية، والقادمة)
- سجل النشاط
- إمكانية تحديث البيانات

### 4. نظام Rate Limiting
- حد أقصى 10 عمليات بحث في الساعة من نفس IP
- حد أقصى 50 عملية بحث يومياً من نفس IP
- حظر تلقائي عند تجاوز الحدود
- تتبع كامل لجميع عمليات البحث

### 5. كشف النشاط المشبوه
الكشف التلقائي عن:
- أكثر من 10 عمليات بحث في 5 دقائق
- أكثر من 15 عملية بحث فاشلة في ساعة
- أكثر من 20 رقم هوية مختلف في ساعة (هجوم Scanning)

### 6. دعم CAPTCHA (اختياري)
- دعم Google reCAPTCHA v3
- قابل للتفعيل والتعطيل من لوحة الإدمن
- إعدادات قابلة للتخصيص:
  - Site Key و Secret Key
  - عتبة النتيجة المقبولة
  - تطبيق على البحث أو إدخال كلمة المرور

### 7. دعم OTP عبر واتساب (اختياري)
- قابل للتفعيل والتعطيل من لوحة الإدمن
- خيارات متعددة:
  - OTP عند البحث
  - OTP عند عرض التفاصيل
  - OTP عند إنشاء كلمة المرور
- مدة صلاحية قابلة للتخصيص

## 🗄️ قاعدة البيانات

### الجداول الجديدة

#### 1. public_search_logs
تسجيل جميع عمليات البحث العام:
```sql
- id: uuid (المعرف الفريد)
- national_id: text (رقم الهوية المبحوث عنه)
- ip_address: text (عنوان IP)
- user_agent: text (معلومات المتصفح)
- search_success: boolean (نجاح العملية)
- beneficiary_found: boolean (العثور على المستفيد)
- captcha_verified: boolean (التحقق من CAPTCHA)
- otp_verified: boolean (التحقق من OTP)
- details: jsonb (تفاصيل إضافية)
- created_at: timestamptz (وقت الإنشاء)
```

#### 2. search_rate_limits
تتبع معدل البحث لكل IP:
```sql
- id: uuid
- ip_address: text (عنوان IP الفريد)
- search_count: integer (عدد عمليات البحث)
- blocked_until: timestamptz (وقت انتهاء الحظر)
- last_search_at: timestamptz (آخر عملية بحث)
- created_at: timestamptz
- updated_at: timestamptz
```

#### 3. suspicious_activity_log
تسجيل النشاط المشبوه:
```sql
- id: uuid
- activity_type: text (نوع النشاط)
- ip_address: text
- national_id: text
- severity: text (low, medium, high, critical)
- description: text
- details: jsonb
- is_reviewed: boolean
- reviewed_by: text
- reviewed_at: timestamptz
- created_at: timestamptz
```

### الدوال الجديدة

#### 1. public_search_beneficiary()
دالة البحث العام الآمن:
```sql
CREATE FUNCTION public_search_beneficiary(
  p_national_id text,
  p_ip_address text,
  p_user_agent text
) RETURNS jsonb
```

#### 2. check_search_rate_limit()
التحقق من معدل البحث:
```sql
CREATE FUNCTION check_search_rate_limit(
  p_ip_address text
) RETURNS jsonb
```

#### 3. log_public_search()
تسجيل عملية البحث:
```sql
CREATE FUNCTION log_public_search(
  p_national_id text,
  p_ip_address text,
  p_user_agent text,
  p_search_success boolean,
  p_beneficiary_found boolean,
  p_details jsonb
) RETURNS uuid
```

#### 4. detect_suspicious_activity()
كشف النشاط المشبوه:
```sql
CREATE FUNCTION detect_suspicious_activity(
  p_ip_address text
) RETURNS boolean
```

### سياسات RLS الجديدة

```sql
-- السماح بالبحث العام المحدود
CREATE POLICY "public_search_beneficiaries_limited"
  ON beneficiaries FOR SELECT TO anon USING (true);

-- السماح بقراءة الطرود
CREATE POLICY "public_read_packages_for_search"
  ON packages FOR SELECT TO anon USING (true);
```

## 🔧 الخدمات (Services)

### publicSearchService.ts

```typescript
// البحث عن مستفيد
searchBeneficiary(nationalId, ipAddress, userAgent)

// التحقق من Rate Limit
checkRateLimit(ipAddress)

// الحصول على التفاصيل الكاملة
getFullBeneficiaryDetails(beneficiaryId, nationalId, pin)

// إنشاء كلمة مرور
createPIN(beneficiaryId, nationalId, pin)

// التحقق من وجود كلمة مرور
checkIfHasPIN(beneficiaryId)

// الحصول على سجلات البحث
getSearchLogs(limit)

// الحصول على النشاط المشبوه
getSuspiciousActivities(limit)

// وضع علامة على نشاط كمراجع
markActivityAsReviewed(activityId, reviewedBy)

// الحصول على إحصائيات البحث
getSearchStats()
```

## 🎨 المكونات (Components)

### 1. PublicSearchResult
عرض نتيجة البحث المحدودة:
- اسم المستفيد
- رقم الهوية
- الطرد قيد التوصيل
- زر "عرض كافة التفاصيل"

### 2. PINCreationModal
نافذة إنشاء كلمة المرور:
- إدخال 6 أرقام
- تأكيد كلمة المرور
- التحقق من قوة كلمة المرور
- إرشادات واضحة

### 3. PINVerificationModal
نافذة التحقق من كلمة المرور:
- إدخال 6 أرقام
- عداد المحاولات
- تنبيه عند القفل
- رابط استرجاع كلمة المرور

### 4. PublicSearchSecurityPage
صفحة إدارة الأمان في لوحة الإدمن:
- إعدادات الأمان العامة
- إعدادات CAPTCHA
- إعدادات OTP
- إحصائيات البحث

## 🚀 كيفية الاستخدام

### للمستفيدين

#### 1. البحث الأولي
```
1. افتح الصفحة الرئيسية
2. أدخل رقم الهوية الوطنية (9 أرقام)
3. اضغط "بحث"
4. ستظهر المعلومات الأساسية والطرد قيد التوصيل
```

#### 2. عرض التفاصيل الكاملة (المرة الأولى)
```
1. اضغط زر "إنشاء كلمة مرور وعرض التفاصيل"
2. أدخل كلمة مرور من 6 أرقام
3. أعد إدخال كلمة المرور للتأكيد
4. احفظ كلمة المرور في مكان آمن
5. ستظهر جميع التفاصيل الكاملة
```

#### 3. عرض التفاصيل الكاملة (المرات اللاحقة)
```
1. اضغط زر "عرض كافة التفاصيل"
2. أدخل كلمة المرور المحفوظة
3. ستظهر جميع التفاصيل الكاملة
```

### للمدراء

#### إعدادات الأمان
```
1. افتح لوحة الإدمن
2. اذهب إلى "إعدادات أمان البحث العام"
3. اضبط الإعدادات المطلوبة:
   - الحد الأقصى للبحث (ساعة/يوم)
   - عدد محاولات كلمة المرور
   - مدة القفل
   - تفعيل/تعطيل الميزات
4. احفظ الإعدادات
```

#### تفعيل CAPTCHA
```
1. احصل على Site Key و Secret Key من Google reCAPTCHA
2. افتح صفحة إعدادات الأمان
3. فعّل CAPTCHA
4. أدخل المفاتيح
5. اضبط عتبة النتيجة (0.5 موصى بها)
6. احفظ الإعدادات
```

#### تفعيل OTP
```
1. تأكد من إعداد نظام واتساب API
2. افتح صفحة إعدادات الأمان
3. فعّل OTP
4. اختر متى يُطلب OTP:
   - عند البحث
   - عند عرض التفاصيل
   - عند إنشاء كلمة المرور
5. احفظ الإعدادات
```

#### مراجعة النشاط المشبوه
```
1. افتح صفحة إعدادات الأمان
2. انظر إلى عدد الأنشطة المشبوهة
3. راجع السجلات
4. اتخذ الإجراءات المناسبة
```

## 🔒 الأمان

### طبقات الحماية

1. **Row Level Security (RLS)**
   - تقييد الوصول على مستوى قاعدة البيانات
   - سماح محدود للمستخدمين غير المصادقين

2. **Rate Limiting**
   - منع إساءة الاستخدام
   - حظر تلقائي عند التجاوز

3. **كلمة المرور (PIN)**
   - حماية البيانات الحساسة
   - تشفير آمن
   - نظام محاولات محدود

4. **كشف النشاط المشبوه**
   - مراقبة مستمرة
   - تنبيهات تلقائية
   - تسجيل كامل

5. **CAPTCHA (اختياري)**
   - حماية ضد الروبوتات
   - Google reCAPTCHA v3

6. **OTP (اختياري)**
   - التحقق بخطوتين
   - عبر واتساب

### أفضل الممارسات

1. **للمستفيدين**
   - استخدم كلمة مرور قوية (تجنب التسلسلات البسيطة)
   - احفظ كلمة المرور في مكان آمن
   - لا تشارك كلمة المرور مع أحد

2. **للمدراء**
   - راجع سجلات البحث بانتظام
   - راجع النشاط المشبوه فوراً
   - اضبط الحدود حسب الحاجة
   - فعّل CAPTCHA في حالة هجمات الروبوتات
   - فعّل OTP للحساسية العالية

## 📊 الإحصائيات

تتوفر إحصائيات شاملة في لوحة الإدمن:
- عدد عمليات البحث اليوم
- عدد عمليات البحث هذا الأسبوع
- عدد عمليات البحث هذا الشهر
- نسبة النجاح
- عدد الأنشطة المشبوهة غير المراجعة

## 🐛 استكشاف الأخطاء

### المشكلة: "حدث خطأ أثناء البحث"
**الحل:**
1. تحقق من اتصال قاعدة البيانات
2. تحقق من سياسات RLS
3. راجع سجلات الأخطاء

### المشكلة: "تم قفل الحساب"
**الحل:**
1. انتظر مدة القفل (30 دقيقة افتراضياً)
2. أو اتصل بالدعم لإلغاء القفل

### المشكلة: "تجاوزت الحد الأقصى من عمليات البحث"
**الحل:**
1. انتظر حتى نهاية الفترة (ساعة أو يوم)
2. أو اتصل بالدعم لزيادة الحد

## 📝 الملاحظات المهمة

1. **التوافق مع المتصفحات**: يعمل النظام على جميع المتصفحات الحديثة
2. **الأداء**: تم تحسين جميع الاستعلامات للأداء العالي
3. **القابلية للتوسع**: النظام جاهز للتعامل مع آلاف المستخدمين
4. **الصيانة**: تنظيف تلقائي للسجلات القديمة (يمكن إضافته لاحقاً)

## 🔄 التحديثات المستقبلية المقترحة

1. استرجاع كلمة المرور عبر OTP
2. تغيير كلمة المرور
3. إشعارات الدخول عبر واتساب
4. سجل تفصيلي لمحاولات الدخول
5. تقارير أمنية شهرية
6. تكامل مع أنظمة SIEM

## 📞 الدعم

للحصول على المساعدة:
- واتساب: +970 59 950 5699
- البريد الإلكتروني: support@example.com

---

## ✅ تم الإنجاز

- [x] إصلاح سياسات RLS
- [x] إنشاء جداول الأمان
- [x] دوال البحث الآمن
- [x] خدمة publicSearchService
- [x] مكونات PIN
- [x] مكون PublicSearchResult
- [x] تحديث LandingPage
- [x] صفحة إدارة الأمان
- [x] نظام Rate Limiting
- [x] كشف النشاط المشبوه
- [x] دعم CAPTCHA
- [x] دعم OTP
- [x] التوثيق الشامل

**تاريخ الإنجاز**: 11 نوفمبر 2025
**الإصدار**: 2.0.0
